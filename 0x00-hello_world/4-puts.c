#include <stdio.h>
/**
 * main - Entry point
 *
 * Description - This is the warning / reminder.
 *
 * return:  always void(success); when all is well.
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
