#include <stdlib.h>
#include <time.h>
#include<stdio.h>
/**
 *main-Entry point
 *Description:numbers and letters with various fetures
 *Return:Always 0(Success)
 */
int main(void)
{
	int b;

	for (b = 48; b <= 57; b++)
	{
		putchar(b);
		if (n == 57)
		{
			continue;
		}
		putchar(',');
		putchar(' ');
	}
	putchar('\n');
	return (0);
}

